
// ("./fs") Local Module - ("fs") Core Module
const fs = require("fs")
const {readFile} = require("fs")

console.log(fs);

// fs.readFile('path', "utf-8", (err, data) => {

// })

// readFile('path', "utf-8", (err, data) => {

// })
